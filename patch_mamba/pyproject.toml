[project]
name = "patch-mamba-training-module"
version = "0.1.0"
description = "Modular Patch-Mamba training framework for maritime trajectory forecasting."
requires-python = ">=3.9"

[tool.setuptools]
packages = ["patch_mamba_training"]
